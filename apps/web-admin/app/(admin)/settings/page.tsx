import PlaceholderPage from '@/components/admin/PlaceholderPage';

export const metadata = { title: 'Configuración | TiendaOnline Admin' };

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Configuración"
      description="Ajustes generales del sistema, integraciones y preferencias."
      module="Settings"
    />
  );
}
