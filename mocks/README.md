# Mocks

Ubicación oficial de simuladores locales del proyecto.

## Estado actual

- El mock FEL/SAT canónico ya vive en `fel-sat-mock/`.
- La copia en `../TiendaOnline_Master_Plan/mocks/fel-sat-mock/` se conserva como fuente heredada y referencia documental.
- `../Program.cs` se mantiene solo como referencia histórica temporal.

## Regla vigente

Para ejecución local y trabajo futuro sobre el mock FEL/SAT, usar:

- `mocks/fel-sat-mock/`
